#  replace the double spaces from problem with single spaces.
st = "This.is.a.string.with.double..spaces."

doubleSpaces = st.replace("..", ".")  # it replaces the doublespaces with the single spaces, (.) is given to know the spacing.

print(doubleSpaces)