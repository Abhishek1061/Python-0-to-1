#Tuples - Tuples is kinda list but it cannot be updated. 
# A tuples is an immutable(cannot change) data type in python.

#Creating a tuple using()
t= (1,2,4,5)
t1 = () # Empty tuple
# t1= (1) # wrong way to declare a Tuple with single element.
t1= (1,) # Tuple with single element.
print(t1) 

# # printing the elements of a tuple
# print(t[0])

# # Cannot update the values of a tuples
# t[0] = 34 # Thows an error.