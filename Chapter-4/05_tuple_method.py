# Creating a tuple using ()

t= (1,2,4,5,4,1,2,1,1)

print(t.count(1)) # it counts the number of charaector which is reocurred.
print(t.index(5)) # it shows us the charector or number present at the placed of mentioned index.
